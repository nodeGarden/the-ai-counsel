# Ports
# Backend (FastAPI + MCP SSE) listens here; the frontend calls it at this port.
PORT_BACKEND=7001
# Frontend (Vite dev server / preview) listens here.
# Avoid 7000 and 5000 on macOS — AirPlay Receiver (ControlCenter) binds those.
PORT_FRONTEND=7002

# Optional: API keys can also be set in the app's Settings UI.
# OPENROUTER_API_KEY=
# TINYFISH_API_KEY=

# Optional: comma-separated browser origins allowed to call the API.
# FRONTEND_HOST=
# Optional: full backend URL when the frontend is served from elsewhere.
# BACKEND_HOST=
